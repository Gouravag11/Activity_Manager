<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ActivityManager" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="backgroundColor">@color/secret_society</item>
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>
    <style name="actMan" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="backgroundTint">@color/secret_society</item>
        <item name="colorPrimary">@color/anecdote</item>
        <item name="android:background">@color/secret_society</item>
        <item name="android:backgroundTint">@color/secret_society</item>

    </style>

    <style name="onactMan" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/gunmetal</item>
        <item name="backgroundTint">@color/gunmetal</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:backgroundTint">@color/gunmetal</item>
    </style>
</resources>